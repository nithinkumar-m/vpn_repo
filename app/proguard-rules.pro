# Keep default rules enabled via build.gradle configuration.
# Add project specific obfuscation rules below if minify is turned on.

-keep class com.cursor.vpn.** { *; }
-keep class androidx.lifecycle.** { *; }
